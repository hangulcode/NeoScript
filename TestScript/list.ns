import dump;

fun Fun_List()
{
	var values = [1, 2, 3];
	values.append(4);
	values.append(0, 0);
	values.resize(3);
	print("list : " .. dump.object(values));
	print("len : " .. values.len());
}

Fun_List();
