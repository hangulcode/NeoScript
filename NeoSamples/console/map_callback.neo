
var g_sData = {};
export fun GetMap() { return g_sData; }

export fun update(var a, var b)
{
	var sum_1 = g_sData.sum(a, b);
	var mul_1 = g_sData.mul(a, b);

	print(sum_1);
	print(mul_1);

	return sum_1 + mul_1;
}
