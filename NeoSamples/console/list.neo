
export fun main()
{
	var Map1 = { 2, 4, 6, 8, 10, 12 };

	foreach(var i, v in Map1)
		print(v);

	var List1 = [ 2, 4, 6, 8, 10, 12 ];
	var List2 = [ 1, 3, 5, 7 ];

	var Set1 = set(List1);

//	for(var i in 0, List1.len(), 1)
//		print(tostring(List1[i]));


	foreach(var i,v in Set1)
		print(tostring(i), " ");
/*

	foreach(var k in List2)
		print(tostring(k));

	var List3 = List1 + List2;

	foreach(var k in List3)
		print(tostring(k));*/
}
