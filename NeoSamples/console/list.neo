
export fun main()
{
	var List1 = [ 5, 3, 1, 4, 7, 2 ];
	var List2 = [ 3, 9, 6, 7 ];

	for(var i in 0, List1.len(), 1)
		print(tostring(List1[i]));

	print("\n");

	foreach(var k in List2)
		print(tostring(k));

	var List3 = List1 + List2;

	print("\n");
	foreach(var k in List3)
		print(tostring(k));
}
