
export fun main()
{
	var tableNumber = [ 5, 3, 1, 4, 7, 2 ];

	for(var i in 0, tableNumber.len(), 1)
	{
		print(tostring(tableNumber[i]));
	}

	foreach(var k in tableNumber)
	{
		print(tostring(k));
	}
	return 1;
}
