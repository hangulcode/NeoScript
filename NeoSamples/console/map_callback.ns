
export var g_sData;

export fun update(var a, var b)
{
/*	var sum_1 = g_sData.sum(a, b);
	var mul_1 = g_sData.mul(a, b);

	print(sum_1);
	print(mul_1);*/

	var pos = g_sData.Transform;
	print("Pos : " .. pos.x .. "," .. pos.y .. "," .. pos.z);

	pos.x = 2.0;

//	g_sData.Transform.x = 2;

	//g_sData.Transform = pos;

	g_sData.Transform = pos;
//	print("Pos : " .. pos.x .. "," .. pos.y .. "," .. pos.z);

	//return pos.x + pos.y + pos.z;
}
