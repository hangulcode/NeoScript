

import fun Mul(var a, var b);
import fun Sample1();



fun Sum1(var a, var b)
{
	return a + b;
}


export fun Sum(var a, var b)
{
/*	Sample1();

	var sum_1 = Sum1(a, b);
	print(sum_1 .. "\n");

	var mul_1 = Mul(a, b);
	print(mul_1 .. "\n");



	return sum_1 + mul_1;*/

	var str = "abcd";
	var len = str.len();

	var tableNumber = [ 5, 3, 1, 4, 7, 2 ];

	for(var i in 0, 5, 1)
	{
		//print(tostring(i));
		print(tostring(tableNumber[i]));
	}

	return 1;
}
